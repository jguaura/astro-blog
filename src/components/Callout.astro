---
type Props = {
  link: string;
  text: string;
}

const { text, link } = Astro.props;
---

<div class="bg-gray-100 flex justify-center py-10 rounded-xl">
  <a href={link} target="_blank" class="bg-teal-900 text-white py-2 px-3 rounded-lg text-lg no-underline">{text}</a>
</div>